// Intentionally not valid Scala. The scala-cli-nix lockfile and build
// must scope to src/ only — if this file leaks into the build, compilation
// fails. Used by the scala3-subset example to verify subset locking.
this is not valid scala
